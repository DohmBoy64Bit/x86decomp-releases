TOOLKIT ARCHITECTURE MAP — 0.7.5

┌─────────────────────┐
│ Immutable ingestion │
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│ Static-analysis     │
│ plane               │
└──────┬───────┬──────┘
       │       │
       │       └──────────────────────────────┐
       │                                      │
┌──────▼──────────────┐             ┌─────────▼──────────────┐
│ Project control     │◄ Governance │ Local LLM proposal     │
│ plane               │             │ plane                  │
└──────┬──────────────┘             │ - LM Studio            │
       │                            │ - Ollama               │
       ├───────────────────────────►│ - llama.cpp server     │
       │                            │ - vLLM                 │
       │                            │ - LocalAI              │
       │                            │ - OpenAI-compatible    │
       │                            └─────────┬──────────────┘
       │                                      │ untrusted JSON/C
┌──────▼──────────────────────────────────────▼──────┐
│ Candidate-generation plane ◄──── Reconstruction   │
└────────────────────────────┬───────────────────────┘
                             │
┌────────────────────────────▼───────────────────────┐
│ Compiler and linker plane ◄──── Assembly           │
└────────────────────────────┬───────────────────────┘
                             │
┌────────────────────────────▼───────────────────────┐
│ COFF extraction and explicit relocation resolution│
└───────────────┬───────────────────────┬────────────┘
                │                       │
┌───────────────▼──────────────┐  ┌─────▼──────────────────────────┐
│ Matching-decompilation lane  │  │ Functional-decompilation lane │
│ raw resolved byte identity   │  │ bounded behavioral validators │
└───────────────┬──────────────┘  └─────┬──────────────────────────┘
                │                       │
                └───────────┬───────────┘
┌───────────────────────────▼──────────────┐
│ Acceptance, evidence, and feedback       │
│ - exact hashes and reports               │
│ - bounded compiler/byte feedback to LLM  │
│ - no model self-verification             │
└──────────────────────────────────────────┘

LOCAL MODEL TRUST BOUNDARY
┌────────────────────────────────────────────────────┐
│ Loopback-only bounded HTTP by default              │
│ Same-origin redirects                              │
│ Environment-variable secrets                       │
│ Structured JSON response contract                  │
│ Exactly one C function definition                  │
│ No inline assembly, byte emitters, or placeholders │
└────────────────────────────────────────────────────┘

UNIFIED COMMAND PLANE
┌───────────────┐
│ x86decomp CLI │
└───────┬───────┘
        ├── core commands
        └── 34 groups / 181 routes
             ├── Governance
             ├── Reconstruction
             │    └── llm: 8 canonical routes
             ├── Native PE
             └── Assembly

MATCHING STATES
not_started -> instruction_similar -> full_relink_validated

FUNCTIONAL STATES
not_started -> differentially_validated -> symbolically_bounded -> integration_validated

MAJOR PLANES
Immutable ingestion
Static-analysis plane
Project control plane
Candidate-generation plane
Compiler and linker plane
Matching-decompilation lane
Functional-decompilation lane
Acceptance, evidence, and feedback
