# Changelog

## 0.7.8

- Added adapter-capability reporting to the comprehensive test harness.
- Added `lm-studio-http` as a loopback OpenAI-compatible endpoint adapter.
- Added `x86decomp-test capabilities` and `capabilities.json` harness output.
- LM Studio can now satisfy `openai-compatible-chat` coverage without being misreported as Ollama, vLLM, LocalAI, or llama.cpp server.
- Preserved the existing universal decompilation acceleration command surface and Dracula site style.

Earlier release history is retained in release archives and source-control history rather than being duplicated in the active site.
