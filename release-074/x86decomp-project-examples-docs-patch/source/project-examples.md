# Project examples

## Example: end-to-end hybrid project

A hybrid project keeps the program continuously buildable while each function moves independently through matching and functional validation. The rendered HTML page in this patch contains the full workflow, command examples, state records, security boundaries, and diagrams.

The canonical deployed source for this patch is `payload/project-examples.html`. This Markdown file is included as an editorial source note rather than a site generator input, because the current documentation package is a hand-built static HTML site.
