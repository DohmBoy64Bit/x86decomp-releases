---
title: x86decomp decompme-pack
description: The v0.7.4 parser declares this command without additional descriptive
  help text.
original_path: commands/decompme-pack.html
---

<a id="command-decompme-pack"></a>

Section: Command reference

# `x86decomp decompme-pack`

The v0.7.4 parser declares this command without additional descriptive help text.

Metadata: current · root command · 1 runnable path

## Help

```
x86decomp decompme-pack --help
```

Metadata: current · core

## `x86decomp decompme-pack`

The v0.7.4 parser declares this command without additional descriptive help text.

### Usage

```
x86decomp decompme-pack [-h] [--overwrite] artifact_dir output_dir
```

### Syntax example

```
x86decomp decompme-pack example ./output
```

### Arguments

| Argument | Source-declared parser metadata |
| --- | --- |
| `artifact_dir` required · type: _path | No argument help text is declared; parser destination is `artifact_dir`. |
| `output_dir` required · type: _path | No argument help text is declared; parser destination is `output_dir`. |
| `--overwrite` nargs: 0 · default: False | No argument help text is declared; parser destination is `overwrite`. |

> **Source basis.** Parser definition: `src/x86decomp/cli.py`; SHA-256 `21e0654ced2f5dd0588adcbedec328613fba524ff1e0a91ef07d63cbbf88288c`. Descriptions above use only parser-declared text or an explicit no-help notice.
