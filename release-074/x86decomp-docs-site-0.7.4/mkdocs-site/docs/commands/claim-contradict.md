---
title: x86decomp claim-contradict
description: The v0.7.4 parser declares this command without additional descriptive
  help text.
original_path: commands/claim-contradict.html
---

<a id="command-claim-contradict"></a>

Section: Command reference

# `x86decomp claim-contradict`

The v0.7.4 parser declares this command without additional descriptive help text.

Metadata: current · root command · 1 runnable path

## Help

```
x86decomp claim-contradict --help
```

Metadata: current · core

## `x86decomp claim-contradict`

The v0.7.4 parser declares this command without additional descriptive help text.

### Usage

```
x86decomp claim-contradict [-h] project claim_id evidence_id
```

### Syntax example

```
x86decomp claim-contradict ./work example-001 example-001
```

### Arguments

| Argument | Source-declared parser metadata |
| --- | --- |
| `project` required · type: _path | No argument help text is declared; parser destination is `project`. |
| `claim_id` required | No argument help text is declared; parser destination is `claim_id`. |
| `evidence_id` required | No argument help text is declared; parser destination is `evidence_id`. |

> **Source basis.** Parser definition: `src/x86decomp/cli.py`; SHA-256 `21e0654ced2f5dd0588adcbedec328613fba524ff1e0a91ef07d63cbbf88288c`. Descriptions above use only parser-declared text or an explicit no-help notice.
