---
title: x86decomp.reconstruction.capsules
description: No module docstring is declared in the v0.7.4 source.
original_path: features/x86decomp-reconstruction-capsules.html
---

<a id="function-capsules-init"></a>
<a id="function-capsules-create"></a>
<a id="function-capsules-inspect"></a>
<a id="function-capsules-verify"></a>
<a id="function-capsules-reproduce"></a>

Section: Source-derived feature and function reference

# x86decomp.reconstruction.capsules

No module docstring is declared in the v0.7.4 source.

Metadata: reconstruction · current · 5 functions/methods

**Source:** `src/x86decomp/reconstruction/capsules.py`

> **Truth basis.** Module and symbol names, signatures, line numbers, docstrings, and SHA-256 are read directly from the sealed source. When source docstrings are absent, this page says so instead of inferring behavior. Source SHA-256: `8aad0081c5ebfc21b3af7484313045cf2a7327d83cb833b8e7c7bcdee865bdf3`.

## Functions and methods

Metadata: internal · line 12

### Capsules.__init__

No function or method docstring is declared in the v0.7.4 source.

```
def __init__(self, store: ReconstructionStore)
```

**Catalog symbol:** `x86decomp.reconstruction.capsules:Capsules.__init__`

Metadata: public · line 13

### Capsules.create

No function or method docstring is declared in the v0.7.4 source.

```
def create(self, name: str, output: str | Path, *, include: list[str], external_requirements: list[dict[str, Any]] | None = None, actor: str = 'analyst') -> dict[str, Any]
```

**Catalog symbol:** `x86decomp.reconstruction.capsules:Capsules.create`

Metadata: public · line 29

### Capsules.inspect

No function or method docstring is declared in the v0.7.4 source.

```
def inspect(self, path: str | Path) -> dict[str, Any]
```

**Catalog symbol:** `x86decomp.reconstruction.capsules:Capsules.inspect`

Metadata: public · line 33

### Capsules.verify

No function or method docstring is declared in the v0.7.4 source.

```
def verify(self, path: str | Path) -> dict[str, Any]
```

**Catalog symbol:** `x86decomp.reconstruction.capsules:Capsules.verify`

Metadata: public · line 47

### Capsules.reproduce

No function or method docstring is declared in the v0.7.4 source.

```
def reproduce(self, path: str | Path, destination: str | Path) -> dict[str, Any]
```

**Catalog symbol:** `x86decomp.reconstruction.capsules:Capsules.reproduce`
