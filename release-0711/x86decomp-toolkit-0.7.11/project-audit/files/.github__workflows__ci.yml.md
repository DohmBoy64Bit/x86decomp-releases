# Per-file audit — .github/workflows/ci.yml

## A. Identity
- Path: `.github/workflows/ci.yml`
- SHA-256: `99a936711b46f589c3af298cb9f8be9a258646b0f56fd5aa530049c7338d6bb6`
- Size: 2906 bytes | Type: text | Classification: build or packaging (CI)
- Batch: B01/B02 (root metadata & docs) | Reviewed: 2026-07-10 | Read completely: yes

## B. Function
3 jobs: regression (ubuntu+windows × py3.11/3.12/3.13; editable install with extras; validate-contracts; `pytest -q`; test-suite self-tests), packaging (build wheels/sdists, clean venv install, smoke `--help` for both CLIs), comprehensive-harness (editable installs; `x86decomp-test init-config` non-interactive non-strict; `x86decomp-test run`; upload artifacts).

## C. Correctness
- `permissions: contents: read` — good least-privilege. Actions pinned to major tags (@v4/@v5), not SHAs — supply-chain note, Informational.
- Regression job runs `python -m pytest -q` (monolithic) — exactly the invocation the release docs admit never completed in the dev sandbox (TEST-001). Whether it passes in CI is Blocked from verification (no CI logs in tree; not a git repo).
- comprehensive-harness writes `./x86decomp-test.json` and `./test-results` into the workspace — consistent with the contamination pattern found in this tree (REPO-004): the documented/automated flows and the ignore rules disagree.
- CI never runs ruff or pyright though both are configured in pyproject; pyflakes gate lives inside validate-contracts per SECOND_AUDIT_FIX (verify in B10).

## L. Findings
- TEST-001 (Medium, Strongly supported): the only full-suite pytest invocation is CI-only and upstream evidence shows monolithic runs timing out in the dev environment; release evidence relies on file-by-file segmented runs. Full-suite behavior on a clean machine is unproven by shipped evidence.

## M. Verdict
Quality: good CI design overall. Priority: medium. Final status: Audited — complete.